<!-- side nav bar -->
<div class="container-fluid">
  <div class="row">
    <div class="col-3 border-right">
  <%= render 'shared/sidebar' %>
    </div>
    <!-- right side page -->
    <div class="col-9">
        <div class="d-flex justify-content-around flex-column mt-4">
       <% @dogs.each do |dog| %>
        <div class="d-flex justify-content-around align-items-center">
          <div>
            <% if dog.photos %>
            <%= cl_image_tag dog.photos.first.key, class: "dog-avatar m-4" %>
            <% end %>
          </div>
          <div class="mt-3 mb-3">
            <h3><%= dog.name %></h3>
           <p><%= dog.breed %></p>
           <!-- add button for remove dog -->
           <%= link_to 'Remove', dog_path(dog), method: :delete, class: "btn-pawpals" %>
          </div>
          </div>
          <%= render "dogeditform", dog: dog %>
        <% end %>
        </div>
    </div>
  </div>
</div>
